# claude ASR 输入

apps\tuya_t5_pocket\tuya_t5_pocket_ai\src\game_ai_chat.c:33 可以获得设备的ASR 数据。

把 ASR 数据使用蓝牙发送到 claude，根据 UI 页面选择进入的会话，把会话 ID 和 ASR 数据发送到 claude 后台。

后台通过蓝牙接收 ASR 数据，然后发送给 claude 后台。根据会话 ID 将 ASR 数据发送到指定的会话中。

分析整个链路的可行性，给出方案。

# UI 更新与改进

1、在主页面的 project 名字字体大小加大，方便用户识别。

2、现在缺少饼状图展示。

3、status 页面的数据新增可以获取的，删除无法获取的。

数据通过 插件从 C:\Users\maida\.claude 路径下的文件获取。

4、上下键选中会话时候会进行反色显示，但是后面闪烁的黑点没有反色显示。

5、根据 C:\Users\maida\.claude 文件尽可能多的展示内容。

# LVGL 中加入中文支持。

有些会话审批内容名字涉及中文汉字，参考 apps\tuya.ai\your_chat_bot\src\display2\ui_wechat\ui.h 的 LV_FONT_DECLARE(ui_font_puhui_18_2); 在当前工程中为 lvgl 加入中文字体以供显示。

# step1

1、apps\tuya_t5_pocket\tuya_t5_pocket_ai\src\display\ui\screen_manager.h 中删除 screen_tab 函数，四个页面切换到额时候 使用 screen_load 切换，左右按键或者返回键切换页面的时候直接出栈注销页面。

2、检测有审批的时候，直接切换到审批页面，led 快闪，等待审批。审批之后返回上一次页面。

3、主页面会话后面的黑点会出现上下键选中会话，黑点消失的问题。解决黑点的显示问题。

4、蓝牙数据采用分包发送，每10s 发送一次，更新设备端数据。

5、扫描 C:\Users\maida\.claude 路径下文件,根据json 格式解析文件内容，梳理出可以在设备上展示的信息在status 页面展示。